import { createContext } from "react";

// Create a context for managing todo-related data
const todoContext = createContext();

// Export the todoContext so that it can be used in other components
export default todoContext;
